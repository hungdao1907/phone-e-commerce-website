const { SMARTPHONE_PRODUCTS } = require('./frontend/src/pages/smartphone/data/mockSmartphoneProducts.ts');
// Actually, it's a TS file. I'll just write a script that fetches the TS file, extracts JSON, or I can just use node and ts-node to run it.
